<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .active { color: green; }
        .inactive { color: red; }
    </style>
</head>
<body>
    <h1>{{ title }}</h1>
    <p>Total Users: {{ users|length }}</p>
    <ul>
        {% for user in users %}
        <li>
            {{ user.name }} ({{ user.role }}) -
            <span class="{% if user.is_active %}active{% else %}inactive{% endif %}">
                {% if user.is_active %}Active{% else %}Inactive{% endif %}
            </span>
        </li>
        {% endfor %}
    </ul>
</body>
</html>